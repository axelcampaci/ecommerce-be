{
  "name": "ecommerce-be",
  "version": "1.0.0",
  "description": "Exercise project for reading from an order list and display some info about it.",
  "main": "out/App.js",
  "scripts": {
    "start": "npm run build && node out/App.js assets/List.csv",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.2",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "csv-parse": "^5.5.2"
  }
}
